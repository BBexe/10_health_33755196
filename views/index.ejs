<%- include('header') %>
    <h1>Welcome to <%= title %></h1>
    <p>Your fitness journey starts here.</p>
    
    <% if (typeof user !== 'undefined' && user) { %>
        <p>Hello, <%= user.username %>! <a href="/dashboard">Go to Dashboard</a></p>
    <% } else { %>
        <p>Please login or register to continue.</p>
    <% } %>
<%- include('footer') %>
